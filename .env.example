# MongoDB connection for insights (packages/insights-cms).
# Needed at build time as well as runtime — pages are pre-rendered from it.
# On Vercel, set both for Production and Preview.
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=praxis
